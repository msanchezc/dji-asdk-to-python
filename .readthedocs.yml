version: 2
sphinx:
    configuration: docs/conf.py

python:
  version: 3.8
  install:
    - requirements: requirements_dev.txt
    - method: setuptools
      path: .

# This is the default, you can omit this
formats: []
